# @layers/web

Main Layers web application.

## Status

**Not yet configured** — This is a placeholder for the Next.js application.

## Setup (when ready)

```bash
# From this directory
pnpm create next-app . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
```

## Will Include

- Next.js 14+ with App Router
- TypeScript
- Tailwind CSS
- @layers/ui components
- @layers/hooks for state
- @layers/core utilities
