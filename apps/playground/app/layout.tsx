import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Toaster } from '@/components/ui/toaster';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Layers Playground',
  description: 'Interactive AI playground demonstrating Layers API integration with full authentication, rate limiting, and credit tracking.',
  keywords: ['AI', 'API', 'playground', 'Claude', 'GPT', 'Gemini', 'chat'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        {children}
        <Toaster />
      </body>
    </html>
  );
}
